import { sql } from '../../database/db-utilities.js';

// -----------------------------------------------------------------
// SELECT QUERY
// -----------------------------------------------------------------
/**
 * Prende l'id di un'utente tramite l'id della sessione
 * @param {*} idSession 
 */
export function currentUser(idSession) {

}

/**
 * Prende l'id del currentUser tramite currentUser() e controlla se è admin
 * @param {*} idCurrentUser 
 */
export function isAdmin(idCurrentUser) {

}